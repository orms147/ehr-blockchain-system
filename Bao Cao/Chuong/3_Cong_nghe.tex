\documentclass[../quyen]{subfiles}

\begin{document}

% ============================================================
% CHƯƠNG 3 — CÔNG NGHỆ SỬ DỤNG (7 mục flat theo reference style)
% ============================================================

\section{Mật mã đối xứng AES-GCM và bất đối xứng NaCl}
\textit{\textbf{<TODO Stage G — AES-256-GCM (NIST SP 800-38D) cho mã hoá nội dung hồ sơ + NaCl box x25519/xsalsa20-poly1305 (Bernstein 2009) cho niêm phong khoá AES cho từng người nhận. Keccak-256 cho cidHash + EIP-712 digest. Cite \cite{nist2007aesgcm}, \cite{bernstein2009nacl}>}}

\section{Blockchain Ethereum và Layer 2 Arbitrum}
\textit{\textbf{<TODO — EVM cơ bản (Wood Yellow Paper 2014). Optimistic Rollup Arbitrum (Kalra 2022). Tại sao Arbitrum Sepolia testnet cho demo. Cite \cite{wood2014ethereum}, \cite{arbitrum2022nitro}>}}

\section{Smart Contract Solidity 0.8 và Foundry}
\textit{\textbf{<TODO — Solidity 0.8.24 + Foundry toolchain (forge build/test/script). Bitwise role flags pattern (gas efficient). 5 contract trong dự án. Cite contracts/foundry.toml>}}

\section{EIP-712 typed data và Sponsor Relayer}
\textit{\textbf{<TODO — EIP-712 spec + domain separator + replay protection qua nonce. Meta-transaction pattern: patient ký typed data, backend submit tx + trả gas. Quota 100 sponsored sigs/tháng/patient. Cite \cite{eip712} + backend/src/services/relayer.service.js>}}

\section{The Graph indexer và IPFS Pinata}
\textit{\textbf{<TODO — The Graph subgraph thay vì RPC poll (tránh 429 storm). 4 dataSources × handler. IPFS content-addressed storage CID = hash content. Pinata pinning service. Cite \cite{thegraph}, \cite{pinata}>}}

\section{Mobile React Native + Expo + Web3Auth}
\textit{\textbf{<TODO — RN + Expo SDK 55 dev client (KHÔNG Expo Go). Web3Auth Sapphire v8.1.0 với 7 providers. Tamagui design system. TanStack Query. Cite \cite{reactnative}, \cite{expo}, \cite{tamagui}, \cite{web3auth}>}}

\section{Backend Node.js + Express + Prisma + PostgreSQL}
\textit{\textbf{<TODO — Node.js + Express middleware pattern. Prisma ORM + PostgreSQL (Neon serverless free tier 5 conn). Socket.io real-time + Expo push. viem RPC client. Cite backend/src/app.js, backend/prisma/schema.prisma>}}

\end{document}
