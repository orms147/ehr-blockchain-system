\documentclass[../quyen]{subfiles}

\begin{document}

% ============================================================
% CHƯƠNG 4 — THIẾT KẾ, TRIỂN KHAI VÀ ĐÁNH GIÁ HỆ THỐNG
% Structure theo reference TOC: 4.1 / 4.2 / 4.3 / 4.4 / 4.5
% Chương lõi (30-35 trang)
% ============================================================

\section{Thiết kế kiến trúc}

\subsection{Lựa chọn kiến trúc phần mềm}
\textit{\textbf{<TODO Stage G — Lý do chọn kiến trúc 3-layer + blockchain Layer 2: trade-off centralized vs decentralized, lý do KHÔNG dùng Ethereum mainnet (gas đắt), lý do KHÔNG dùng private chain (mất tính phi tập trung), Arbitrum Sepolia là sweet spot cho thesis demo>}}

\subsection{Thiết kế tổng quan}
\textit{\textbf{<TODO — Embed figures/02-component-3-layer.png (Hình 4.1) + figures/03-deployment.png (Hình 4.2). Mô tả 3-layer Mobile / Backend / Blockchain + 4 service bổ trợ: IPFS, Subgraph, Web3Auth, Postgres>}}

\subsection{Thiết kế chi tiết gói}
\textit{\textbf{<TODO — Mô tả các package: mobile/src/{screens-v2, services, components-v2, utils, store} + backend/src/{routes, services, middleware, config} + contracts/src/{5 contract files}. Bảng package + mô tả mỗi cái 1 dòng>}}

\section{Thiết kế chi tiết}

\subsection{Thiết kế giao diện}
\textit{\textbf{<TODO — Embed screenshots key screens: LoginScreen / DashboardScreen / RecordsScreen / ShareSheet / RecordDetailScreen / RequestsScreen / DelegationScreen / EditProfileScreen / TrustedContactsScreen. 8-10 figure}}

\subsection{Thiết kế lớp Smart Contract}
\textit{\textbf{<TODO — Embed figures/04-class-contracts.png (Hình 4.X). Mô tả 5 contract: AccessControl / RecordRegistry / ConsentLedger / DoctorUpdate / EHRSystemSecure. Mỗi contract: 1 paragraph state vars chính + function public + events emit. Cite source Solidity>}}

\subsection{Thiết kế cơ sở dữ liệu}
\textit{\textbf{<TODO — Embed figures/05-er-prisma.png (Hình 4.X). Liệt kê 10+ model Prisma: User / DoctorProfile / RecordMetadata / KeyShare / Consent (cache) / TrustedContact / OrganizationMember / AccessRequest / DelegationAccessLog / Notification. Invariant note: Consent table chỉ cache — luôn check on-chain canAccess. Cite backend/prisma/schema.prisma>}}

\section{Xây dựng ứng dụng}

\subsection{Thư viện và công cụ sử dụng}
\textit{\textbf{<TODO — Bảng libraries used: cột [Layer | Library | Version | Mục đích]. Cover mobile (RN, Expo, Tamagui, Web3Auth, viem, expo-image-picker, nacl-tweetnacl, AsyncStorage, SecureStore) + backend (Express, Prisma, viem, socket.io, expo-server-sdk, zod) + contracts (Foundry, OpenZeppelin)>}}

\subsection{Kết quả đạt được}
\textit{\textbf{<TODO — Bảng kết quả: 5 contract deploy live Arbitrum Sepolia + địa chỉ + block deploy / Mobile app 27 screens / Backend 15 REST routes / Subgraph v0.1.5 với 4 dataSources / 100 forge test (96 pass). Số liệu code: lines of code + commit count>}}

\subsection{Minh hoạ các chức năng chính}
\textit{\textbf{<TODO — 4-5 sequence diagram + screenshots:
- figures/06-seq-grant-consent.png (Cấp quyền)
- figures/07-seq-request-access.png (Yêu cầu truy cập 3 bước)
- figures/08-seq-trusted-contact.png (Trusted Contact)
- figures/09-seq-revoke-cascade.png (Thu hồi cascade)
Mỗi flow: 1 paragraph mô tả + sequence diagram + 1-2 screenshot mobile thực tế>}}

\section{Kiểm thử}

\subsection{Forge test smart contract}
\textit{\textbf{<TODO — Liệt kê 9 test file (contracts/test/*.t.sol). Bảng kết quả: 96/100 pass (4 fail là setUp() baseline trong test files outdated, không block code mới). Liệt kê critical test groups: AccessControl / ConsentLedger / ConsentLedgerPhase1Fixes (Footgun #1) / ConsentLedgerTrustedContact (Footgun #2) / ConsentLedgerDelegation / RecordRegistry / DoctorUpdate / EHRSystemSecure. Cite test_DirectGrantClearsDelegationSource, test_CanAccess_TrustedContact_BypassesNormalConsent>}}

\subsection{Load test backend với k6}
\textit{\textbf{<TODO — USER FILL từ load-test/RESULTS.md sau khi chạy 3 scenario:
Scenario 1 Light (10 VUs × 10min): bảng metric p50/p95/p99/throughput/error
Scenario 2 Medium (50 VUs × 10min): bảng metric
Scenario 3 Stress (200 VUs × 5min): bảng metric + verify rate limit kick in 429
Bottleneck analysis: RPC quota / DB pool / event loop
Conclusion: app handle X concurrent users với p95 < Y ms>}}

\subsection{Kiểm thử thủ công golden path}
\textit{\textbf{<TODO — Bảng test case manual: 6 critical flow đã test (Patient grant + Doctor claim + Ministry create org + ...). Reference memory project_test_checklist.md. Result: PASS/FAIL/PENDING với note>}}

\section{Triển khai}

\subsection{Deploy smart contract Arbitrum Sepolia}
\textit{\textbf{<TODO — Lệnh forge script + 5 contract address mới (2026-06-01 redeploy với Footgun fix): AccessControl 0x5Fb8...30f2 / ConsentLedger 0xBFEb...D55a / RecordRegistry 0x910d...82F5 / EHRSystemSecure 0xC17c...1f43 / DoctorUpdate 0x2549...D33f. Block 272771506-272771532. Lệnh sync ABI + .env post-deploy>}}

\subsection{Deploy subgraph The Graph Studio}
\textit{\textbf{<TODO — Lệnh graph deploy ehr. Endpoint: https://api.studio.thegraph.com/query/120096/ehr/v0.1.5. 4 dataSources × address+startBlock. Reindex time ~5-10 phút>}}

\subsection{Deploy backend và mobile}
\textit{\textbf{<TODO — Backend hiện local dev. Production target: VPS/Docker container. Mobile dev build qua Expo dev client; APK production qua eas build --platform android. Cite Phụ lục A>}}

\end{document}
