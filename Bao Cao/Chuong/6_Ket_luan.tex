\documentclass[../quyen]{subfiles}

\begin{document}

% ============================================================
% CHƯƠNG 6 — KẾT LUẬN VÀ HƯỚNG PHÁT TRIỂN
% Theo reference: 6.1 Kết luận + 6.2 Hướng phát triển
% ============================================================

\section{Kết luận}

\textit{\textbf{<TODO Stage G — Recap kết quả đạt được + transparent về hạn chế:

Kết quả đạt được:
- 5 smart contract Solidity 0.8.24 deploy live trên Arbitrum Sepolia (cite địa chỉ contract Chương 4.5.1)
- Ứng dụng mobile React Native demo end-to-end 4 vai trò: Patient/Doctor/OrgAdmin/Ministry
- Backend Node.js Express + PostgreSQL với 15 REST endpoint + Socket.io real-time
- Subgraph indexer The Graph Studio v0.1.5
- 96/100 smart contract test pass (Foundry)
- Tuân thủ 5 văn bản pháp luật trọng yếu của Bộ Y tế Việt Nam (Luật KCB 15/2023, TT 13/2025, TT 32/2023, TT 26/2025, TT 13/2026, QĐ 586/QĐ-BYT)
- Đáp ứng <X> người dùng đồng thời theo kết quả load test k6 (Chương 4.4.2)

Đóng góp khoa học (4 contributions chi tiết ở Chương 5):
1. Kiến trúc Envelope Encryption + Blind Mailbox
2. Cơ chế Uỷ quyền dây chuyền + Thu hồi cascade (Footgun #1 fix)
3. Người thân tin cậy On-chain (Footgun #2 fix)
4. Patient zero-gas qua Sponsor Relayer + Biometric MFA

Hạn chế hiện tại:
- Chưa tích hợp HL7 FHIR (liên thông quốc tế)
- Chưa hỗ trợ DICOM (ảnh chẩn đoán hình ảnh thuần)
- 29 mẫu HSBA chuyên khoa TT 32/2023 chưa template hoá đầy đủ (đồ án dùng generic form)
- Chưa xuất XML BHYT QĐ 4210 (out of scope — không thanh toán BHYT)
- Chưa tích hợp PKI chữ ký số NEAC (thay bằng EIP-712 — đóng góp khoa học, nhưng chưa tương đương pháp lý)
- 4 test contract setUp() failing (pre-existing baseline, không block code mới)
- Subgraph indexer phụ thuộc The Graph Studio (centralized service)
- Mobile app chưa build APK production (dev client)>}}

\section{Hướng phát triển}

\subsection{Tích hợp tiêu chuẩn liên thông y tế}
\textit{\textbf{<TODO — HL7 FHIR R4 export/import + DICOM imaging server + XML BHYT QĐ 4210 (nếu mở rộng sang thanh toán)>}}

\subsection{Hoàn thiện 29 mẫu HSBA chuyên khoa}
\textit{\textbf{<TODO — Theo TT 32/2023 Phụ lục XXVIII: nội khoa / ngoại khoa / sản khoa / nhi khoa / RHM / TMH / mắt / da liễu / tâm thần / ...>}}

\subsection{Tích hợp PKI chữ ký số NEAC}
\textit{\textbf{<TODO — Bổ sung EIP-712 (không thay), cho phép bệnh viện chấp nhận chữ ký theo Luật Giao dịch điện tử>}}

\subsection{Tích hợp VNeID}
\textit{\textbf{<TODO — Sử dụng VNeID identity layer cho registerAsPatient flow — mở rộng theo Chỉ thị 17/CT-TTg>}}

\subsection{Multi-chain support}
\textit{\textbf{<TODO — Ethereum mainnet + Polygon + zkSync — multi-bridge consent>}}

\subsection{Decentralized indexer}
\textit{\textbf{<TODO — Federated subgraph trên The Graph Network mainnet, giảm phụ thuộc Studio centralized>}}

\end{document}
